<html>
    <head>
        <title>{% block title "Admin"%}</title>
    </head>
    
    <body>
        <h1>{{ block("title") }}</h1>
        <div>
            <a href="{{ path('admin_logoutadmin') }}">Se déconnecter</a>
            <a href="{{ path('admin_userlist') }}">Liste de users</a>
        </div>

        <div class="content">
            {% block content %}{% endblock %}
        </div>
    </body>    
</html>